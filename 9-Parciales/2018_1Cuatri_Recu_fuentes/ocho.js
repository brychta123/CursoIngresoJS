function mostrar()
{
    var numero;    
    var numeroUno;
    var contador;
    contador=0;
    while(contador=2)
    {
    numero=prompt("Ingrese el primer numero");
    numero1=prompt("Ingrese el segundo numero");
    numero=parseInt(numero);
    numero=parseInt(numeroUno);
    while(numero<-100 || numero>100)
    {
        numero=prompt("Ingrese de nuevo el numero");
        numero=parseInt(numero);
    }        
    contador++;    
    }
    
        document.write("que onda");
    
        
}
